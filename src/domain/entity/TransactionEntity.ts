export type TransactionEntity = { 
    idAccount: number, 
    value: number, 
    typeTransaction: string,
    codeTransaction: string,
    description: string,
    createdAt: string,
    updatedAt: string,
}