export type TransactionEntity = { 
    idAccount: number, 
    value: number, 
    typeTransaction: string,
    codeTransaction: string,
    description: string,
    active: boolean,
    createdAt: string,
    updatedAt: string,
}